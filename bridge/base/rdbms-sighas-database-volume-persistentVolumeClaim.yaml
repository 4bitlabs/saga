#! rdbms-sighas-database-volume-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: rdbms-sighas-database-volume
    namespace: sighas-api
    labels:
        com.docker.compose.service: rdbms
        com.docker.compose.volume: sighas-database-volume
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
